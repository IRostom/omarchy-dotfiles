# irostom.workspaces

Personal clone of Omarchy's built-in workspaces bar-widget
(`omarchy.workspaces`), created with `omarchy plugin clone omarchy.workspaces`.

Currently functionally identical to upstream — kept as a local, namespaced
clone so it can be tracked and tweaked independently of Omarchy updates.

## License

MIT — see `LICENSE`.
