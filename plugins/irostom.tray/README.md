# irostom.tray

Personal clone of Omarchy's built-in system tray bar-widget
(`omarchy.tray`), created with `omarchy plugin clone omarchy.tray`.

Currently functionally identical to upstream — kept as a local, namespaced
clone so it can be tracked and tweaked independently of Omarchy updates.

## License

MIT — see `LICENSE`.
